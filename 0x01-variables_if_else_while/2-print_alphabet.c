#include <stdio.h>
/**
 * main - Prints the alphabet in lower case
 *only use putchar
 * Return: (0)
 */
int main(void)
{
char ch;

for (ch = 'a' ; ch <= 'z' ; ch++)

putchar(ch);
printf("\n");

return (0);
}
