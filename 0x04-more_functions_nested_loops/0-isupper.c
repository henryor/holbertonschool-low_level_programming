#include "main.h"

/**
 * _isupper - check for uppercase character
 * @c: variable
 * Return: (0)
 * auth: Henry Ortegon G.
 **/

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
